const name = "Francisco"; // string

let hpPoints: number | string = 95; // number or string

const isAlive: boolean = true; // boolean   

hpPoints = "FULL"; // valid assignment since hpPoints can be a string

console.log({name,hpPoints, isAlive});
 

export {};